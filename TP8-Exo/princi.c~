#include <stdio.h>
#include "Def.h"

int main()
{
  
    eltpers Tab[MAXPERS];
    float somme=0;
    
    saisie(Tab);
    SommeNets(Tab);
    
    
    printf("Somme des salaires nets = %.2f\n", somme);
    return 0;
    
}